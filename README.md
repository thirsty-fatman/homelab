# homelab
Setup script to apply to Ubuntu Server 24.04 LTS. Installs, executes prerequisites and runs Docker with Portainer and DockGE.
One of the two (Portainer or DockGE) will be discarded in a newer release.
